---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: litellm-postgresql
  annotations:
    cnpg.io/skipEmptyWalArchiveCheck: enabled
spec:
  instances: 1

  # renovate: datasource=docker packageName=ghcr.io/cloudnative-pg/postgresql
  imageName: ghcr.io/cloudnative-pg/postgresql:18.3

  storage:
    size: 20Gi
    storageClass: ${DEFAULT_STORAGE_CLASS}

  stopDelay: 300
  smartShutdownTimeout: 60

  resources:
    requests:
      cpu: 20m
      memory: 256Mi
    limits:
      memory: 512Mi

  enableSuperuserAccess: true
  managed:
    roles:
      - name: litellm
        ensure: present
        comment: Litellm
        login: true
        superuser: true
        passwordSecret:
          name: litellm-database

  bootstrap:
    # initdb:
    #   database: litellm
    #   owner: litellm
    #   encoding: UTF8
    #   secret:
    #     name: litellm-database
    recovery:
      source: litellm-postgresql-backup
      database: litellm
      owner: litellm
      secret:
        name: litellm-database

  postgresql:
    shared_preload_libraries:
      - vchord.so

  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: litellm-postgresql-scaleway
        serverName: postgresql

  externalClusters:
    - name: litellm-postgresql-backup
      plugin:
        name: barman-cloud.cloudnative-pg.io
        parameters:
          barmanObjectName: litellm-postgresql-scaleway
          serverName: postgresql
