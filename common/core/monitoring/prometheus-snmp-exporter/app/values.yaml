---
extraArgs:
  - "--config.file=/config/snmp.yml"

extraSecretMounts:
  - name: config
    mountPath: /config
    secretName: snmp-config
    readOnly: true
    defaultMode: 420

service:
  ipDualStack:
    enabled: true

configmapReload:
  enabled: true
  extraArgs:
    - "--watched-dir=/config"
  extraVolumeMounts:
    - name: config
      mountPath: "/config"
      readOnly: true
  resources:
    requests:
      cpu: 3m
      memory: 43Mi
    limits:
      memory: 43Mi

resources:
  requests:
    cpu: 3m
    memory: 27Mi
  limits:
    memory: 27Mi
