# network
---
apiVersion: v1
kind: Service
metadata:
  name: omada-controller-web
spec:
  type: ClusterIP
  ipFamilyPolicy: PreferDualStack
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
