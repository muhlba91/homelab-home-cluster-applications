---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: omada-controller-mongodb-user
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-auth
  target:
    name: omada-controller-mongodb-user
    template:
      engineVersion: v2
      data:
        password: "{{ .password }}"
        # the operator's own auto-generated *-conn-str secret requires TLS,
        # and neither app trusts the operator's self-signed internal CA -
        # explicitly disable TLS here rather than relying on default behavior
        uri: "mongodb://omada:{{ .password }}@omada-controller-mongodb-rs0:27017/omada?replicaSet=rs0&authSource=omada&tls=false"
  data:
    - secretKey: password
      remoteRef:
        conversionStrategy: Default
        decodingStrategy: None
        key: omada-controller-mongodb-user
        property: password
