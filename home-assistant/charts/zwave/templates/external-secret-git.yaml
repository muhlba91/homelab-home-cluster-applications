---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: zwave-js-git
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: doppler-auth-home-assistant
  target:
    name: zwave-js-git
  data:
    - secretKey: gitlab-token
      remoteRef:
        key: GITLAB_TOKEN
    - secretKey: git-crypt-key
      remoteRef:
        key: ZWAVE_GIT_CRYPT_KEY
