---
global:
  projectEnvironment: production
  environment: prod
  spec:
    helm:
      valueFiles:
        - values.yaml
        - values-production.yaml
    enableAutomated: false # TODO: once we are confident in this working, enable automated deployments
